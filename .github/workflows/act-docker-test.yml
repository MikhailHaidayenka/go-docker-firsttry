name: actions-test

on:
  push:
    branches: [ "master" ]


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: build image
#      - run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
#      - run: docker build -t ghcr.io/go-docker-firsttry:$(date +%s) .
        run: docker build -t ghcr.io/go-docker-firsttry:latest .

      - name: push image
        run: docker push ghcr.io/go-docker-firsttry:latest .
